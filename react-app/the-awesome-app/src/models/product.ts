
// class , interface and type alias are used to define the shape of an object in TypeScript.
export interface Product {
    id: number;
    name: string;
    price: number;
    description: string;
    imageUrl: string;
}
